spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
server:
  port: 8100
  servlet:
    context-path: /auth
springdoc:
  version: '1.0'
  swagger-ui:
    path: /auth
tech:
  vtsign:
    api-url: http://localhost:8765
